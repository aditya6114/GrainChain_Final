# Dependencies — installed fresh inside the container
node_modules

# Build output — generated fresh inside the container
.next

# Backend has its own Dockerfile
backend/node_modules
backend/dist

# Environment files — NEVER bake secrets into images
.env
.env.*
backend/.env
backend/.env.*

# Git and editor files
.git
.gitignore
.vscode
.idea
*.swp
.DS_Store

# Docker files
Dockerfile
.dockerignore
docker-compose.yml

# Docs and config not needed at runtime
README.md
SYSTEM_DESIGN.md
supabase/
*.md
